[build]
base = "frontend"
command = "npm ci && npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

## Removido VITE_API_URL para usar chamadas relativas + proxies no _redirects
